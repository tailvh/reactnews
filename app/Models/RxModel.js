import Realm from 'realm'


/*RxModel.write(()=> {
  RxModel.create('Dog', {name: 'Honda', id: this.guid(), cat_id: curItem.id.toString()})
})
catPosts[listMenus[index].id] = RxModel.objects('Dog').filtered('cat_id = "' +listMenus[index].id.toString()+ '"').slice(0, 100)
*/
/*
let realm = new Realm({ schema : [{name: 'Post', primaryKey: 'id', properties: {
  id: 'string',
  code: 'string',
  name: 'string',   
  pics: 'string',
  site: 'string',
  time: 'string',
  relative: 'string',  
  cat_id: 'string'
}}], schemaVersion: 1  });

export default realm*/